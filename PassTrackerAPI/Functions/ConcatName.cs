namespace PassTrackerAPI.Functions
{
    public class ConcatName
    {
        public static string ConcatNameFunc(string secondName, string firstName, string thirdName)
        {
            return secondName + " " + firstName + " " + thirdName;
        }
    }
}
